
spring:
  application:
    name: persons # app name need equals name config file persons.yml
  profiles:
    active: dev
  cloud:
    config:
      uri: http://localhost:8888

#eureka:
#  client:
#    service-url: http://localhost:8761/eureka
#  instance:
#    prefer-ip-address: true
server:
  port: 8081
