package org.example.dependencies;

//public class Picture extends Draw {  bad extends because class dependecies from specific class
public class Picture implements Drawable {
    // class must have dependencies from abstraction or interface
    // but not specific class
}
