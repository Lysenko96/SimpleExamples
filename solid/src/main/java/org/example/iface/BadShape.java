package org.example.iface;

public interface BadShape {

    // all class that implements this interface must realise all method even if class not need all of them

    void drawCircle();

    void drawRectangle();
}
